---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - argocd-cm-patch.yaml

# Strategic merge patch - only updates specified keys in argocd-cm
# Preserves other ConfigMap data managed by ArgoCD
